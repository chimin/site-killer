{
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "site-killer",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel"
  }
}
